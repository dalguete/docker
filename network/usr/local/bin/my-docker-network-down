#!/bin/sh

# The args passed are:
# $1 - hostname
# $2 - ip

# Remove the entry found in /etc/my-docker-network-hosts file, with data passed
sed -i "/^$2 $1\.docker\s/d" /etc/my-docker-network-hosts

# Restart dnsmasq service
service dnsmasq restart


